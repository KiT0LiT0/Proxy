namespace ProxyShellReady.Models
{
    public enum ConnectionMode
    {
        WholeComputer,
        AppSelection
    }

    public enum ContentTab
    {
        Rules,
        Apps
    }

    public enum LocalProxyMode
    {
        MixedSystemProxy,
        Tun
    }
}
